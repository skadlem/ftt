# Test Plan
Generated by /plan-eng-review on 2026-09-04
Branch: unknown (repo pending: skadlem/ftt)
Repo: skadlem/ftt (to be created — T1)

No HTTP surface. "Routes" below are CLI entrypoints; /qa runs them, not a browser.

## Affected Entrypoints/Commands
- `python tasks/mine.py --pmos-root ~ --out fixtures/` — mining: output must reject dirty/duplicate/oversized tasks (assert counts in stderr)
- `python tasks/split.py --eval-n 50 --seed <pinned>` — freeze eval set; MUST raise non-zero exit on any train/eval overlap
- `python harness/judge.py --pairs pairs.jsonl --out verdicts.jsonl` — blind pairwise; verify source stripped from prompts and order randomized (seeded)
- `python harness/gate.py --gate G0 --verdicts verdicts.jsonl --eval-hash <frozen>` — gate refuses to run when eval-set hash != frozen hash
- `python teacher/sample.py --tasks pool.jsonl --model qwen3.8-max --resume` — kill mid-run, rerun with --resume, assert no duplicate traces and complete count

## Key Interactions to Verify
- Two-pass swap: same pair judged A|B and B|A → flip detected and dropped; flip-rate reported
- Judge validation set: judge must score >=90% agreement with known-good/known-bad labels before any gate runs
- Kaggle notebook: session restart → adapter checkpoints on disk survive (download, don't trust `/kaggle/working` alone)

## Edge Cases
- Task with plan output > context budget → sampler truncates visibly or rejects, never silently
- Judge API 500/timeout → retry x3, then abort run loudly (non-zero exit, partial verdicts NOT scored)
- Empty .pmos tree from a mined project → mining skips with warning, exit code unchanged
- Eval set size < 50 after disjointness filtering → split.py errors, does not proceed with n=43

## Critical Paths
- End-to-end dry run on 5 seed pairs: mine → split → judge (two-pass) → gate G0 emits win-rate + p-value + flip-rate, all reproducible from pinned seeds
- Contamination guard: hand-place one task into both pools → split.py must raise (regression test, CRITICAL)
